jsonrpc.handler:
  path: '/jsonrpc'
  defaults:
    _controller: '\Drupal\jsonrpc\Controller\HttpController::resolve'
  methods: [POST]
  requirements:
    _permission: 'use jsonrpc services'
  options:
    _auth: ['basic_auth']